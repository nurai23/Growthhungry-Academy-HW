tasks=[]

def dispay_menu():

    print("1.Add a new task")
    print("2.View all tasks")
    print("3.Delete a task by its name")
    print("4.Delete a task by its number")

    def Add_a_new_task():
        "Adds a new task to the list"
        task=input("Enter the task")
        task.append({"task:task, completed":False})
        print(f"Task '{task}' added.")

    def View_all_tasks():
        "Displays all tasks with their status."
        if not tasks:
            print("No tasks in the list")
            return # ask why return   
        
    def Delete_a_task_by_its_name():
        "Delete a task from the list"
        view_tasks(tasks)
        if not tasks:
            return
        
    def Delete_a_task_by_its_number():
        "Delete a task by its number"
        view_tasks(tasks)
        if not tasks:
            return
    def main():
        "Main function to run to do list"
        while True:
            dispay_menu()
            choice=input("Enter your number from 1to 4")    
   
        if choice == "1":
         print("Add_a_new_task")
         task = input()
         tasks.append(task)

        elif choice == "2":
         print("--------\n" "Your tasks are:")
         for task in tasks:
            print(task)
            print("---------\n")

        elif choice =="3":
         print("Delete_a_task_by_its_name:")
         for task in range(len(tasks)):
            print(str(task +1) + ") "+tasks[task])
            Delete_a_task_by_its_name=int(input())

        elif choice =="4":
         Delete_a_task_by_its_number
    
        else:
         print("Invalid task. Please select from 1 to 4")

    



