#def two_sum_brute_force(nums, target):
def twoSum(nums: list[int], target: int) -> list[int]:
    for i in range(len(nums)):
        for j in range(i+1, len(nums)):
            if nums[i] +nums[j] == target:
             return [i, j]
    return[]
x=[45,50,10]
print( twoSum(nums=x, target=95))
